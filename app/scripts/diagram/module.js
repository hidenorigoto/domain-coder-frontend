'use strict';

/**
 * @ngdoc overview
 * @name domainCoderApp
 * @description
 * # domainCoderApp
 *
 * Main module of the application.
 */
angular.module('Diagram', [
    'ngCollection'
])
;

var Diagram = {};

if (typeof exports === 'object') {
    module.exports = Diagram;
}
