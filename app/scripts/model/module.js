'use strict';

/**
 * @ngdoc overview
 * @name domainCoderApp
 * @description
 * # domainCoderApp
 *
 * Main module of the application.
 */
angular.module('DomainCoder', [
    'ngCollection', 'uuid'
])
;

var DomainCoder = {

};
if (typeof exports === 'object') {
    module.exports = DomainCoder;
}
